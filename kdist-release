#! /bin/bash

SUMMARY="Manage kernel and project releases"

USAGE="<command> [<args>]"

release__get_parent () {
	git describe --abbrev=0 ${1:-HEAD}~1 2>/dev/null
}

while
	case $1 in
	-*)	usage ;;
	*)	break
	esac
do
	shift
done

kdist_subcommand=$1
shift
kdist__run_handler "$@"
